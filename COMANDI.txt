# Down&Conv - Comandi utili

## Sviluppo
.\scripts\run.ps1

## Release (procedura completa)
# 1. Commit e push codice
git add -A
git status
git commit -m "release: X.Y.Z - descrizione"
git push origin main

# 2. Tag e push — QUESTO AVVIA IL WORKFLOW RELEASE
git tag vX.Y.Z
git push origin vX.Y.Z

# Verifica: github.com/.../actions e .../releases
